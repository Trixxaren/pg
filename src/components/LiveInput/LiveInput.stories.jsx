import LiveInput from "./LiveInput";

export default {
  title: "Components/LiveInput",
  component: LiveInput,
};

export const Default = () => {
  return (
    <div>
      <LiveInput />
    </div>
  );
};
